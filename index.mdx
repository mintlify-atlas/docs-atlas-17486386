---
title: "Asimilation"
description: "A lightweight Node.js backend framework for easily creating routes and handling middlewares"
---

<div className="bg-gradient-to-r from-[#cb0404] to-[#e37b84] text-white rounded-3xl p-12 mb-8 shadow-xl">
  <h1 className="text-5xl font-bold mb-4">Asimilation</h1>
  <p className="text-xl opacity-90 mb-6">
    Build powerful Node.js backends with minimal code. Simple routing, flexible middlewares, and TypeScript support out of the box.
  </p>
  <div className="flex gap-4">
    <a href="/quickstart" className="bg-white text-[#cb0404] px-6 py-3 rounded-full font-semibold hover:bg-[#f3d2d2] transition">
      Get Started
    </a>
    <a href="https://github.com/juanjh1/asimilation" className="bg-transparent border-2 border-white px-6 py-3 rounded-full font-semibold hover:bg-white/10 transition">
      View on GitHub
    </a>
  </div>
</div>

## Why Asimilation?

Asimilation is designed to be the simplest way to build Node.js backends. No boilerplate, no complexity—just clean APIs for routing and middleware management.

<CardGroup cols={2}>
  <Card title="Lightweight" icon="feather">
    Minimal dependencies and a small footprint. Get started in seconds without the bloat.
  </Card>
  <Card title="Dynamic Routing" icon="route">
    Support for static and dynamic paths with parameter types like `/<int:id>` and `/<string:name>`.
  </Card>
  <Card title="Flexible Middlewares" icon="filter">
    Global and route-specific middleware pipeline for authentication, logging, and more.
  </Card>
  <Card title="TypeScript First" icon="code">
    Full TypeScript support with comprehensive type definitions for better DX.
  </Card>
</CardGroup>

## Quick Example

Get a server running in just a few lines:

```typescript index.ts
import { asi, url } from '@asimilation/core';

// Configure the server
asi.setup(3000, import.meta.url);

// Add a simple route
url.addPath("/", (req, res) => {
  res.sendJson({ message: "Hello from Asimilation!" }, 200);
});

// Add a dynamic route
url.addPath("/user/<int:id>", (req, res) => {
  const userId = req.params.id;
  res.sendJson({ userId, name: "John Doe" }, 200);
});

// Start the server
asi.run();
```

## Key Features

<CardGroup cols={3}>
  <Card title="Static & Dynamic Routes" icon="signs-post" href="/core-concepts/routing">
    Handle both fixed paths and parameterized URLs with type validation
  </Card>
  <Card title="Middleware Pipeline" icon="layer-group" href="/core-concepts/middlewares">
    Chain middlewares globally or per-route for maximum flexibility
  </Card>
  <Card title="Route Modules" icon="boxes-stacked" href="/core-concepts/route-modules">
    Organize routes into namespaced modules for better structure
  </Card>
  <Card title="Request/Response Helpers" icon="envelope-open-text" href="/core-concepts/request-response">
    Convenient methods like sendJson() and sendText() for responses
  </Card>
  <Card title="Built-in Middlewares" icon="wrench" href="/middlewares/logger">
    Logger, timeout, and error handling middlewares included
  </Card>
  <Card title="Type Safety" icon="shield-check" href="/installation">
    Full TypeScript definitions for your entire application
  </Card>
</CardGroup>

## Get Started

<Steps>
  <Step title="Install the package">
    Add Asimilation to your project with npm, yarn, or pnpm
    
    ```bash
    npm install @asimilation/core
    ```
  </Step>
  
  <Step title="Create your first server">
    Set up a basic server with routing
    
    ```typescript
    import { asi, url } from '@asimilation/core';
    
    asi.setup(3000, import.meta.url);
    url.addPath("/", (req, res) => {
      res.sendJson({ status: "ok" }, 200);
    });
    asi.run();
    ```
  </Step>
  
  <Step title="Add middlewares and dynamic routes">
    Explore the full power of Asimilation
    
    Read the [Quickstart Guide](/quickstart) to learn more
  </Step>
</Steps>

## Explore the Documentation

<CardGroup cols={2}>
  <Card title="Core Concepts" icon="book" href="/core-concepts/routing">
    Learn about routing, middlewares, and the request/response cycle
  </Card>
  <Card title="Guides" icon="map" href="/guides/creating-routes">
    Step-by-step tutorials for common use cases
  </Card>
  <Card title="API Reference" icon="code" href="/api/asi">
    Complete API documentation for all exports and types
  </Card>
  <Card title="Built-in Middlewares" icon="plug" href="/middlewares/logger">
    Discover the middlewares that come with Asimilation
  </Card>
</CardGroup>
